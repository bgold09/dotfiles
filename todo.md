TODO

dark mode
caps lock map
install git
install curl


## git gcm


```
gpg --quick-generate-key --batch "Brian Golden <bgold09@users.noreply.github.com>"
pass init "Brian Golden <bgold09@users.noreply.github.com>"
git-credential-manager configure
```

autoinstall
install flatpak
  - sudo apt install flatpak
  - flatpak remote-add --if-not-exists flathub https://dl.flathub.org/repo/flathub.flatpakrepo

bitwarden
		flatpak install flathub com.bitwarden.desktop
font
shell?

steam?

clock format
terminal theme
new terminal?

vim
  - fix plug.vim install
  - install from snap: sudo snap install nvim --classic


## copilot

curl -fsSL https://gh.io/copilot-install | bash
    doesn't check if already installed

```bash
# Download and install nvm:
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.40.4/install.sh | bash

# in lieu of restarting the shell
\. "$HOME/.nvm/nvm.sh"

# Download and install Node.js:
nvm install 24

npm install -g @github/copilot
```



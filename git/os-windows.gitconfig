[include]
	path = ~/.local.gitconfig

[core]
	editor = gvim -f

[credential]
    helper = manager-core

[mergetool "diffconflicts"]
	cmd = gvim -c DiffConflicts $MERGED $BASE $LOCAL $REMOTE


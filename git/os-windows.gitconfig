[include]
	path = ~/.local.gitconfig

[core]
	editor = nvim-qt

[credential]
    helper = manager-core

[mergetool "diffconflicts"]
	cmd = nvim-qt +DiffConflicts $MERGED $BASE $LOCAL $REMOTE

